// - Crie uma função chamada somar que recebe dois parâmetros (números) e retorna a soma deles.

function Somar(nota1,nota2) {
    let soma = nota1 + nota2
    console.log(soma);
    
}
Somar()